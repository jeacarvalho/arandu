# 📚 Aprendizado: REQ-01-04-01 — Histórico Farmacológico e Sinais Vitais

**Data:** 2026-03-18  
**Requirement:** REQ-01-04-01  
**Status:** ✅ Implementado

## 🎯 Objetivo

Implementar capacidade de registrar e monitorizar o contexto biopsicossocial do paciente, focando em:
- Histórico de medicação
- Indicadores fisiológicos (sinais vitais)

## 📁 Arquivos Criados/Modificados

### Domínio
- `internal/domain/patient/medication.go` - Entidade Medication
- `internal/domain/patient/vitals.go` - Entidade Vitals
- `internal/domain/patient/medication_test.go` - Testes Medication
- `internal/domain/patient/vitals_test.go` - Testes Vitals

### Infraestrutura
- `internal/infrastructure/repository/sqlite/migrations/0005_biopsychosocial_tables.up.sql`
- `internal/infrastructure/repository/sqlite/migrations/0005_biopsychosocial_tables.down.sql`
- `internal/infrastructure/repository/sqlite/medication_repository.go`
- `internal/infrastructure/repository/sqlite/vitals_repository.go`

### Aplicação
- `internal/application/services/biopsychosocial_service.go`

### Web
- `internal/web/handlers/biopsychosocial_handler.go`
- `web/components/patient/biopsychosocial_panel.templ`
- `web/components/patient/medication_list.templ`
- `web/components/patient/vitals_widget.templ`
- `web/components/patient/biopsychosocial_viewmodel.go`

### Integração
- `cmd/arandu/main.go` - Rotas e inicialização

## 🔑 Padrões Aprendidos

### 1. Validações no Domínio
```go
// Data de início não pode ser futura
if startedAt.After(time.Now()) {
    return nil, errors.New("medication start date cannot be in the future")
}

// Nível de apetite entre 1 e 10
if appetiteLevel != nil && (*appetiteLevel < 1 || *appetiteLevel > 10) {
    return nil, errors.New("appetite level must be between 1 and 10")
}
```

### 2. Status com Transições Válidas
```go
func (m *Medication) Suspend() error {
    if m.Status == MedicationStatusFinished {
        return errors.New("cannot suspend a finished medication")
    }
    // Define ended_at automaticamente
    now := time.Now()
    m.Status = MedicationStatusSuspended
    m.EndedAt = &now
    return nil
}
```

### 3. ViewModels Protegem o Domínio
```go
// Handler mapeia domínio → ViewModel
viewModel := patientComponents.BiopsychosocialPanelViewModel{
    PatientID:     context.PatientID,
    Medications:   toMedicationViewModels(context.AllMedications),
    LatestVitals:  toVitalsViewModel(context.LatestVitals),
}
```

### 4. HTMX para Atualizações Parciais
```html
<button 
    hx-put={ "/patients/" + patientID + "/medications/" + med.ID + "/status?status=suspend" }
    hx-target="#medication-list"
    hx-swap="innerHTML"
>
```

## 📊 Métricas

- **Testes de domínio:** 14 passando
- **Cobertura de validações:** 100%
- **Endpoints HTMX:** 4

## ⚠️ Considerações

1. **ON DELETE CASCADE** - Garantido nas tabelas para integridade referencial
2. **Índices** - Criados para patient_id, status, e date
3. **Responsividade** - Painel pode ser modal em mobile
4. **Multi-tenancy** - Estrutura preparada para isolar por tenant_id (futuro)

## 🔗 Próximos Passos

- [ ] Integrar painel na página de detalhes do paciente
- [ ] Adicionar gráficos de evolução de peso (CAP-09-01)
- [ ] Implementar alertas para alterações significativas
